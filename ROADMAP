Roadmap for ocsvg (SVG editor for ownCloud) as of beta1

beta:
 * PNG export
 * l10n (set language to the current owncloud language)
 * better usage of jQuery UI
 * get rid of "really want to close?" after saving

Ideas for the future:
 * collaborative editing
